# Art staging — мастерская нового арта

Мастерская и **единственный источник истины** для всего арта игры (стиль
«уютный детектив»). Редизайн перенесён в игру; мастерская остаётся навсегда —
все новые ассеты будущих уровней рождаются здесь (см. «Новые ассеты»).

## Структура

| Папка | Что здесь | Куда попадёт в конце |
| --- | --- | --- |
| `items/` | иконки предметов, ключ = имя файла (`chair.svg`) | `src/assets/icons/items/` |
| `themes/` | иконки тем меню (`airport.svg`) | `src/assets/icons/themes/` |
| `persons/` | архетипы `person-(m\|f)-NN.svg` | `src/assets/icons/persons/` |
| `textures/` | текстуры пола (`wood.svg`) | `src/assets/textures/` |
| `tools/` | валидатор, гейт и генератор превью (запускаются из корня репо) | никуда, остаются утилитами |
| `preview/` | сгенерированная локальная галерея (`index.local.html`, локальный файл) | никуда |

Ключи (что рисовать) и приоритеты: `docs/icon-reference.md` в корне репо — там же
частота использования каждого предмета в уровнях.

## Конвенции файлов

Полный рецепт стиля: [STYLE-GUIDE.md](./STYLE-GUIDE.md). Технические конвенции
те же, что и в игре (`docs/assets.md` в корне репо): viewBox 96×96 (текстуры 256×256),
без фильтров/blend/растра/текста, id с префиксом ключа, одежда персонажей через
`fill="var(--person-clothing)"`. Отличия от игровой конвенции:

- лимит веса иконки поднят с 10 до **15 КБ** (текстуры — 24 КБ);
- `opacity` запрещена в предметах/темах/текстурах; в персонажах допустима только
  как затемнение/осветление поверх одежды (`fill="#1A1A1A"` / `#FFFFFF`, ≤ 0.35).

## Рабочий цикл (каждая партия)

```bash
npm run preview-art
open art/preview/index.local.html  # смотреть глазами: 18/40/64px, 3 фона, ч/б,
                                   # все игровые цвета одежды, тайлы 3×3
```

`preview-art` обновляет реестр ключей, проверяет мастерскую, строит галерею и запускает
headless-проверку. Локальный HTML исключён из git.

1. Агент рисует партию файлов по STYLE-GUIDE, ориентируясь на эталоны из пилота.
2. `npm run preview-art` зелёный → превью пересобрано и автоматически проверено.
3. Человек смотрит превью, говорит правки → файлы правятся до утверждения.
4. Утверждённая партия считается эталоном стиля для следующих.

## Новые ассеты для новых уровней

Новый уровень принёс новый предмет / тему / текстуру / архетип? Путь ровно один:

1. **Рисуем в `art/<категория>/`** по `STYLE-GUIDE.md` (палитра, тени, §7 для полов,
   эталоны §6 — копировать приёмы, не изобретать заново). Имя файла = игровой ключ
   (`docs/icon-reference.md`; новый ключ вводится разработкой игры).
2. **Цикл мастерской**: `npm run preview-art` → ревью человеком в
   `art/preview/index.local.html`.
3. **Перенос**: `cp art/<cat>/*.svg src/assets/…` (см. таблицу «Структура»).
4. **Гейт**: `npm run verify` — проверяет уровни, сборку, lint, арт и Playwright; в том
   числе `validate-art` проверяет мастерскую, игру и синхронность между ними.

Запрещено: рисовать/править ассеты сразу в `src/assets/` (гейт увидит рассинхрон
и уронит тесты); добавлять цвета мимо палитры §1 (новый цвет = новый токен с пометкой
использования); копировать приёмы со старых файлов до редизайна.

## Перенос в игру (один раз, в самом конце)

Когда все категории готовы и утверждены:

```bash
cp art/items/*.svg    src/assets/icons/items/
cp art/themes/*.svg   src/assets/icons/themes/
cp art/persons/*.svg  src/assets/icons/persons/
cp art/textures/*     src/assets/textures/
npm run verify
```

После этого (по желанию, отдельным шагом): удаление fallback-свитча в
`ItemIcon.tsx` при 100% покрытии предметов и параметрической фигурки в
`PersonFigureSvg.tsx` при полном покрытии гендеров.

## Статус

- [x] Этап 0 — инструменты и стайлгайд (эта папка, валидатор, превью)
- [x] Этап 1 — пилот утверждён (эталоны: chair, armchair, cactus, person-m-01, person-f-01, wood)
- [x] Этап 2 — персонажи: все 8 архетипов утверждены
- [x] Этап 3 — предметы 120 шт: все 6 батчей утверждены (б6: 6 версий пользователя + 11 наших)
- [x] Этап 4 — темы 26 шт: ТМ1–ТМ3 утверждены (правки ревью: agency+книга, airport без тени, hollywood=clapperboard, stadium топ-даун, station рельсы, wizardschool фиолетовый колпак)
- [x] Этап 5 — текстуры 20 шт: T1 + T2 утверждены (rubber — терракотовая пара палитры, stairs — ступени периодом 128)
- [x] Этап 6 — перенос в игру: 174 файла скопированы, audit-icons 100%, смоук 20/20, `validate-art` гейт подключён к `pretest:smoke`

**Редизайн завершён.** Мастерская продолжает работать: новые ассеты — только через
«Новые ассеты для новых уровней» выше.
